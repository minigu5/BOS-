# BOS 가스 누출 탐지 — 코드 수정 보고서

**대상 저장소**: `github.com/minigu5/BOS-`
**비교 기준**: 최초 커밋 `bbed8b2` (2026-04-25, "first commit") → 현재 코드 `fe1636c` (2026-05-25)
**작성일**: 2026-05-25

---

## 1. 개요

본 프로젝트는 공장 영상에서 **BOS(Background-Oriented Schlieren, 배경지향 슐리렌) 광학 흐름 + 3D CNN**으로
눈에 보이지 않는 가스 누출을 탐지한다. 파이프라인은 전처리(`1_preprocess.py`) → 학습(`2_train.py`)
→ 추론(`3_realtime_detect1.py` / `4_nonrealtime.py`)로 구성된다.

최초 커밋은 일정 정확도(보고상 Test F1 0.90)를 보였으나, **현장 카메라(AX700)로 추론하면 확률이
들쭉날쭉하며 의미 있는 탐지가 되지 않는** 문제가 있었다. 원인을 추적한 결과, 모델 구조가 아니라
**파이프라인이 잡으려는 BOS 신호를 여러 단계에서 스스로 죽이고 있었고, 학습과 추론의 입력이
서로 달랐던 것**이 핵심이었다. 본 보고서는 최초 커밋의 문제점을 단계별로 정리하고, 현재 코드가
이를 어떻게 해결했는지 근거(실측 수치 포함)와 함께 기술한다.

### 최초 커밋의 파일 구성
| 파일 | 역할 |
|------|------|
| `1_preprocess.py` | 영상 → 광학 흐름 청크(.npy) 전처리 |
| `2_train.py` | 3D CNN 학습 |
| `bos_webcam.py` | 웹캠 BOS 시각화 (실질적 추론 스크립트) |
| `2_train.py`/`README.md`/`requirements.txt` | 학습·문서·의존성 |

> 주목: 최초 커밋에는 **`bos_common.py`가 없었다.** 신호 처리 로직이 `1_preprocess.py`와
> `bos_webcam.py`에 **각각 따로(중복)** 구현되어 있었고, 그 내용이 서로 달랐다. 이것이 모든
> 불일치 문제의 근본 구조였다.

---

## 2. 최초 커밋의 문제점

### 2-A. 전처리(`1_preprocess.py`)

**P1. EMA 배경 흐름 — 지속적 플룸을 배경으로 흡수**
신호를 `flow = Farneback(EMA배경, 현재프레임)`으로 계산하고, EMA 배경을 `alpha=0.05`로 갱신했다.
```python
flow = compute_farneback_flow(ema_bg, curr_gray, ...)
ema_bg = (1.0 - alpha) * ema_bg + alpha * curr_gray   # alpha=0.05
```
가스/열기처럼 **계속 흐르는(steady) 플룸**은 약 1~2초 만에 EMA 배경에 흡수되어, 현재 프레임과의
차이(=신호)가 사라진다. 즉 "**켜둔 상태가 곧 배경**"이 되어 신호가 소멸한다. 실제로 열원을 켠
영상과 끈 영상의 BOS 출력이 육안으로도 구분되지 않았다. 또한 EMA는 프레임레이트에 의존하므로
오프라인 전처리와 실시간 추론에서 의미가 달라진다(스큐 유발).

**P2. 224×224 저해상도에서 흐름 계산 — 서브픽셀 신호 소실**
```python
"resize": (224, 224),
```
BOS 왜곡은 원본에서도 서브픽셀~수 픽셀 수준인데, 1920×1080(또는 4K)을 224로 8~17배 축소하면
그 미세 변위가 평균되어 사라진다.
**실측**: 동일 플룸 영역의 흐름 magnitude가 native 해상도 **0.50 → 224에서 0.10**(배경 수준)으로 폭락.
즉 224 다운스케일만으로 신호의 약 80%가 손실되었다.

**P3. 정규화 하한이 사실상 없음 — 노이즈 뻥튀기**
```python
denom = p99 - p1
if denom < 1e-7:        # 1e-7 → 실질적으로 절대 발동하지 않음
    normalized[...] = 0.0
else:
    normalized[...] = clip((ch - p1) / denom * 2 - 1, -1, 1)
```
움직임이 거의 없을 때도 `denom`(1~99 percentile 폭)이 `1e-7`보다는 항상 크므로, **미세 센서
노이즈가 [-1, 1] 전 범위로 확대**된다. 결과적으로 "가만히 있어도 신호가 가득 찬" 청크가 생성되어
정상/가스 구분을 흐린다.

**P4. 오탐 억제·전역 움직임 보정 전무**
최초 전처리는 정규화 외에 어떤 억제도 하지 않는다. 따라서 **카메라 흔들림/미세 드리프트가 그대로
신호로 들어간다.** 실측에서 삼각대 없이 촬영한 영상은 화면 전체가 통째로 밀리는 전역 변위가
신호의 약 2/3를 차지했다(전역 움직임 보정으로 평균 0.33 → 0.107로 감소).

**P5. 저장 해상도 미축소 — 디스크 과다**
청크를 224 해상도 그대로 저장(약 6MB/청크)하여 수만 청크 시 디스크를 크게 점유했다.

**P6. 파일명 기반 라벨링만 지원**
`*_G.mp4` / `*_N.mp4` 파일명 규칙에만 의존하여, 대량 수집·관리가 불편했다.

### 2-B. 학습(`2_train.py`)

**P7. 이중 클래스 균형 → 퇴화 붕괴(degenerate collapse)**
`WeightedRandomSampler`(클래스 균형 샘플링)와 `FPPenaltyBCELoss(fp_weight=2.0)`(Normal 가중)
**두 가지 클래스 균형 기법을 동시에** 적용했다.
```python
train_sampler = make_weighted_sampler(train_l)   # 균형 ①
criterion = FPPenaltyBCELoss(fp_weight=2.0)       # 균형 ②
```
데이터가 부족할 때 이 조합은 모델이 **"전부 한 클래스로 예측"하는 퇴화 상태**에 빠지게 만들었다.

**P8. best 모델 선정이 Val F1 기준 → epoch 1에 고착**
```python
if va_m["f1"] > best_val_f1:   # 퇴화 시 F1=0 → 영원히 갱신 불가
```
퇴화 상태에서는 F1이 0에 박혀 best가 갱신되지 않아, 첫 에포크 모델이 그대로 저장되는 일이 발생했다.

**P9. 퇴화 조기 감지 지표 부재**
출력이 한쪽으로 쏠리는지 보여주는 `mean_prob`/`pos_rate` 같은 진단 컬럼이 없어, 학습이 망가지는
것을 로그만으로는 알아채기 어려웠다.

**P10. 소규모 데이터에 과한 학습 설정**
`lr=1e-3`, `dropout=0.5`, `patience=10`, `weight_decay=1e-4` — 과적합이 빠른 소규모 데이터셋에
비해 학습률이 높고 정규화가 약하며 인내심이 길어, 불안정/과적합을 키웠다.

### 2-C. 추론(`bos_webcam.py`) — 가장 치명적

**P11. 학습-추론 입력 불일치(train/inference skew)**
신호 처리가 두 곳에서 **서로 다르게** 구현되어 있었다.

| 단계 | 학습(`1_preprocess.py`) | 추론(`bos_webcam.py`) |
|------|------------------------|----------------------|
| 해상도 | 224×224로 리사이즈 | 카메라 풀해상도 (리사이즈 없음) |
| 정규화 | `normalize_flow_robust` 적용 | **없음** (raw flow) |
| 기준 프레임 | EMA 배경 | 첫 프레임 고정 + `addWeighted(0.99/0.01)` |

모델은 224·정규화·EMA로 학습됐는데, 추론은 풀해상도·무정규화·다른 기준을 쓴다. **입력 분포가
완전히 달라** 학습된 모델의 출력이 추론 시 신뢰할 수 없게 된다(현장 "확률 들쭉날쭉"의 직접 원인).

**P12. 실제 모델 추론 부재**
`bos_webcam.py`는 광학 흐름을 HSV로 **시각화**하고 `.npy`로 저장만 할 뿐, **학습된 3D CNN으로
판정하는 코드가 없었다.** 즉 end-to-end 탐지 파이프라인이 완성되지 않은 상태였다.

**P13. 신호 처리 로직 중복 → 단일 진실 공급원 부재**
동일한 흐름 처리가 `1_preprocess.py`와 `bos_webcam.py`에 중복 구현되어, 한쪽을 고쳐도 다른 쪽이
어긋났다. 일관성을 코드 구조가 보장하지 못했다.

---

## 3. 현재 코드의 해결 방법

핵심 전략은 **(1) 신호 처리를 한 곳으로 모아 학습=추론을 코드 수준에서 일치시키고, (2) 신호를
죽이던 단계들을 신호 보존 방향으로 재설계**하는 것이었다.

### S-A. `bos_common.py` 신설 — 단일 진실 공급원 (P11·P12·P13 해결)
모든 신호 처리(흐름 계산·GMC·억제·정규화)를 `bos_common.py` 한 곳에 모으고, 전처리(`1_`)와
추론(`3_`/`4_`)이 **글자 그대로 같은 함수**(`process_pair`)를 호출하도록 했다. 한쪽을 바꾸면
양쪽이 자동으로 함께 바뀌므로 train/inference skew가 구조적으로 발생할 수 없다. 또한 추론
스크립트가 실제 3D CNN으로 판정하도록 완성했다.

### S-B. EMA 배경 → 연속 프레임 흐름 + GMC (P1·P4 해결)
지속 플룸을 흡수하던 EMA 배경을 폐기하고, **직전 프레임과 현재 프레임 사이의 흐름**을 본다(삼각대
고정 전제). 카메라 미세 드리프트는 **전역 움직임 보정(GMC, 프레임 전역 중앙값 흐름 차감)**으로 제거한다.
```python
def process_pair(prev_gray_f32, curr_gray_f32, **sup):
    flow = compute_flow(prev_gray_f32, curr_gray_f32)   # 연속 프레임
    flow = suppress_false_positive(flow, **sup)         # (a) GMC ...
    flow_norm = normalize_flow_robust(flow)
    return flow_norm, curr_gray_f32                      # 다음 prev 반환
```

### S-C. 224 → 720 고해상도 흐름, 저장은 112로 축소 (P2·P5 해결)
흐름을 **720 해상도에서 계산**해 서브픽셀 신호를 보존하고, 디스크 절약을 위해 청크는 흐름 계산
**이후** 112로 축소 저장한다(`--save_size 112` 기본). 고해상도에서 추출된 신호가 축소 후에도 유지됨을
실측으로 확인했다.

### S-D. 정규화 하한 강화: `1e-7` → `MIN_DENOM=0.20` (P3 해결)
percentile 폭이 `MIN_DENOM`보다 작으면 해당 채널을 0으로 둔다. 움직임이 없을 때 노이즈가 [-1,1]로
증폭되는 현상을 차단했다.

### S-E. 오탐 억제 도입, 단 플룸은 보존 (P4 해결)
`suppress_false_positive`에 4단계 억제를 두되, 가스 플룸(=크고 연결된 난류)을 죽이지 않도록 튜닝했다.
실측(C0121: 배경 p50≈0.06 / 플룸 p99≈0.94·max≈2.9 / 손 등 max≈40)에 근거한 값이다.
| 단계 | 값 | 목적 |
|------|-----|------|
| GMC | ON | 삼각대 미세 드리프트/팬 제거 |
| 데드존 `DEADZONE_LO` | 0.10 | 배경 노이즈 제거(플룸>0.1 보존) |
| 상한 `CEILING_HI` | 6.0 | 손/사람 등 큰 강체 제거(플룸 max≈2.9 보존) |
| 응집 블롭 `COHERENCE` | **OFF** | 켜면 큰 난류 플룸을 통째로 잘라내므로 기본 비활성 |

### S-F. 폴더 기반 라벨링 추가 (P6 해결)
`input_videos/gas/`, `input_videos/normal/` 폴더로 라벨을 지정(파일명 무관). 기존 `_G`/`_N`도 호환.

### S-G. 학습 안정화 (P7·P8·P9·P10 해결)
| 항목 | 최초 커밋 | 현재 |
|------|-----------|------|
| 클래스 균형 | WeightedRandomSampler + FP penalty(2.0) **이중** | Sampler 제거, `fp_weight=1.0`(표준 BCE) |
| best 선정 | Val **F1** (퇴화 시 고착) | Val **Loss** (퇴화에 강건) |
| 퇴화 진단 | 없음 | `VaMeanP`/`VaPos%` 컬럼 추가 |
| 학습률 / dropout | 1e-3 / 0.5 | **3e-4 / 0.7** |
| patience / wd | 10 / 1e-4 | **3 / 5e-4** |

오탐 억제는 학습 단계의 클래스 가중이 아니라 **추론 임계값(threshold) 스윕**으로 처리하여 학습
퇴화를 방지한다.

### S-H. 추론 스크립트 완성 (`3_`/`4_`)
`bos_common`을 그대로 import하여 학습과 동일한 신호 경로를 타고, 실제 3D CNN으로 판정한다.
ROI 크롭, 해상도/임계값 슬라이더, **6프레임 시간적 히스테리시스 경보**(산발적 깜빡임 무시),
좌(카메라)·우(BOS 히트맵) 분할 UI, 경보음 등을 추가했다. `4_nonrealtime.py`는 녹화 파일을 재생하며
추론하므로 실시간 프레임레이트 스큐가 없어 더 안정적이다.

### S-I. 신호 검증 진단 도구 추가
모델 작업 이전에 **"BOS 신호가 실제로 존재하는가"**를 검증하는 독립 도구를 추가했다.
- `bos_check.py`: 대화형 GUI(억제 OFF, 고해상도/native 흐름, 정적 reference vs EMA 비교, ROI, 배속).
- `bos_analyze.py`: 헤드리스 배치 분석 — 영상 → 시간평균/최댓값 흐름 히트맵 PNG + 시간대별 신호 그래프.
  GMC·OFF기준 정적 reference 비교 옵션 포함.

---

## 4. 검증 결과 (실측)

재설계된 파이프라인을 올바른 촬영 셋업(삼각대 고정 + 소스를 배경 1m 앞 + 고온 라이터)에서 찍은
영상(C0121)으로 끝단 검증했다.

| 검증 항목 | 결과 | 의미 |
|-----------|------|------|
| 해상도 민감도 | 동일 플룸: 720에서 0.50 → **224에서 0.10** | 저해상도가 신호 80% 소실 (P2 입증) |
| 흔들림 보정 | GMC로 평균 0.33 → **0.107** | 신호의 2/3가 리그 흔들림이었음 (P4 입증) |
| 끝단(정규화·112 축소 후) | 가스 플룸 정규화 mean **0.33** | 축소 후에도 신호 보존 |
| 정상/가스 분리 | 정지·노이즈·1px드리프트 = **0.00** vs 플룸 0.33 | 정상은 0, 가스만 살아남아 분리 (P1·P3·P4 동시 입증) |

> 결론: 정상 프레임은 0으로 떨어지고 가스 플룸만 구조적 신호로 남아, **모델이 학습·구분할 수 있는
> 입력**이 만들어졌다.

---

## 5. 정리: 최초 커밋 → 현재

| # | 최초 커밋의 문제 | 현재 코드의 해결 |
|---|------------------|------------------|
| P1 | EMA 배경이 지속 플룸 흡수 | 연속 프레임 흐름 + GMC |
| P2 | 224 저해상도 → 신호 소실 | 720 고해상도 흐름, 저장만 112 |
| P3 | 정규화 하한 부재 → 노이즈 증폭 | `MIN_DENOM=0.20` |
| P4 | 흔들림/드리프트 억제 없음 | GMC + 데드존 + 상한 |
| P5 | 224 청크 저장(디스크 과다) | 흐름 후 112 축소 저장 |
| P6 | 파일명 라벨링만 | 폴더 기반 라벨링 |
| P7 | 이중 클래스 균형 → 퇴화 | Sampler 제거 + fp_weight 1.0 |
| P8 | Val F1 기준 → 고착 | Val Loss 기준 |
| P9 | 퇴화 진단 없음 | VaMeanP/VaPos% 추가 |
| P10 | 과한 학습 설정 | lr·dropout·patience 재조정 |
| P11 | 학습-추론 입력 불일치 | `bos_common` 단일 경로 |
| P12 | 실제 모델 추론 부재 | `3_`/`4_`에서 3D CNN 판정 |
| P13 | 신호 로직 중복 | 단일 진실 공급원 통합 |

### 남은 작업
현재 `checkpoints/best_model.pth`는 **구 파이프라인(224/EMA)으로 학습된 모델이라 무효**다.
재설계된 전처리로 데이터를 다시 수집·전처리하여 **재학습**해야 새 추론 경로가 의미 있는 결과를 낸다.
